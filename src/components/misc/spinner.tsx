import { Spin } from "antd";

interface SpinnerComponentProps {
  color?: string;
  size?: number;
}

export function SpinnerComponent({ color, size = 24 }: SpinnerComponentProps) {
  return (
   <div></div>
  );
}
